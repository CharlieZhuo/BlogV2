## 管理博客文章的元数据并生成文章列表

### 添加元数据

推荐为所有的文章都附带标准格式的元数据

### 在页面显示中移除元数据

### 生成文章列表

在需要文章列表数据的页面，在 Next.js 的 getStaticProps 中调用生成列表的函数，并作为 props 返回。

在这个函数中，将执行：

1. 扫描文章目录下的所有 mdx 文件
2. 提取 YAML front Matter 风格的元数据（我用的是 gray-matter 库来进行）
3. 根据某种顺序（如元数据中的发表时间、访问量等）排序并分页
